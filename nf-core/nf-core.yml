extends: substitution
message: Use '%s' instead of '%s'
level: error
ignorecase: false
action:
  name: replace
swap:
  # TODO Maybe one day
  # https://github.com/docker/docs/blob/main/.github/vale/Docker/Substitute.yml
  # https://github.com/redhat-documentation/vale-at-red-hat/blob/main/.vale/styles/RedHat/Hyphens.yml
  # '(?<!nf-core)\s': nf-core
  "NF-core": nf-core
  "NF_core": nf-core
  "nf_core": nf-core
  "nf-core": nf-core
