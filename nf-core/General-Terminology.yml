extends: substitution
message: "Use proper capitalization: '%s' instead of '%s'"
level: suggestion
link: https://nf-co.re/docs/contributing/guidelines
action:
  name: replace
swap:
  # Miscellaneous terms not covered by other domain-specific rules
  # Most content moved to Tools.yml, Companies.yml, and Branding.yml
  # This file now serves as a catch-all for general terminology